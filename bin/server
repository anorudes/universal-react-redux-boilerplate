#!/usr/bin/env node
'use strict';

if (process.env.NODE_ENV !== 'production') {
  require('babel-polyfill');
  require('babel-register')({
    plugins: [require('./remove-scss')]
  });
}

require('../config');

const server = require('../server');
server.listen(3000);
